package com.example.adlerlife.data.local

import androidx.room.TypeConverter

/**
 * Type converters for Room.
 * Currently not in use as all fields in [com.example.adlerlife.data.model.TraceLog] 
 * are supported natively by Room.
 */
class Converters {
    // Leftover from previous implementation. 
    // This file can be deleted if no custom types are added to the database.
}
